"""
Cross-USAFA data synchronization for the management panel.

Modules:
    snapshot  - serialize a RAC database into a JSON-serializable dict
    merger    - combine per-USAFA snapshots into aggregate stats
    provider  - the seam between the panel and its data source
"""
